# 要件定義 ToDoリスト

**作成日**: 2026年4月7日

---

## Phase 1（優先対応）

### 技術スタック選定

- [x] フロントエンド技術選定 → **Vue 3 + Vite**（Vue Router / Pinia を使用）
- [x] バックエンド技術選定 → **Java / Spring Boot**（Spring Security を使用）
- [x] データベース選定 → **PostgreSQL**
- [x] ホスティング・デプロイ先選定 → **Vercel**（フロント）/ **Railway**（バックエンド+DB、月$5〜）
- [ ] 開発環境構築方針（Docker使用有無など）

---

### API設計書の作成

- [x] REST API / GraphQL どちらにするか決める → **REST API**
- [x] APIのURLプレフィックス・バージョニング方針 → **`/api/v1/`**
- [x] 全エンドポイントの一覧を作成する（メソッド・URL・リクエスト/レスポンス形式）
- [x] レスポンス形式の統一（成功時・エラー時のJSONフォーマット）
- [x] HTTPステータスコードの利用方針（例: 404 vs 400 の使い分け）

---

### バリデーション仕様

- [ ] メールアドレス: フォーマット検証のみか、ドメイン制限も行うか
- [ ] パスワード: 最低文字数・大文字小文字数字の必須要件を定義する
- [ ] 料理名: 最大文字数を定義する
- [ ] ポイント: 小数点以下の桁数（1桁まで？）・0.1〜1.0の検証方法を定義する
- [ ] メモ: 最大文字数を定義する

---

### 認証・セキュリティ

- [x] 認証方式 → **JWT（Spring Security で自前実装）**
- [x] パスワードリセットのトークン方式 → **DBに保存するランダムトークン**（JWTではなく使い捨て）
- [x] JWTの有効期限を確定する → **24時間**
- [x] リフレッシュトークンを導入するか検討する → **Phase 1はなし**（有効期限切れはログインし直し）
- [ ] CORS設定の方針（許可するオリジンを定義）
- [ ] レートリミットを設けるか検討する（特にログイン・パスワードリセット）
- [ ] HTTPS必須かどうか（本番環境要件として明記）

---

### メール送信

- [ ] **利用サービス選定**（SendGrid / AWS SES / Resend など）
  - > 注意: 多くのサービスは無料枠あり（例: SendGrid は月100通まで無料）、超過後は有料になります
- [ ] パスワードリセットメールの本文・テンプレートを作成する
- [x] パスワードリセットトークンの有効期限を確定する → **1時間**

---

### 機能仕様の未確定事項

- [ ] 検索とカテゴリ絞り込みを同時に使えるAND条件を要件定義書に明記する（現状は実装注意にのみ記載）
- [ ] カテゴリの初期データの投入方法を決める（マイグレーション時のシードデータとして定義）
- [ ] 退会機能（Could）: データの扱いを定義する
  - ユーザーの論理削除 or 物理削除か
  - 関連するレシピはどうなるか（一緒に削除？保持？）

---

### 画面設計

- [x] 全画面のワイヤーフレームを作成する
- [x] レスポンシブ対応のブレークポイントを決める → **sm: 640px〜**
- [x] モバイルファースト か デスクトップファーストか方針を決める → **モバイルファースト**
- [x] UIライブラリを選定する → **Tailwind CSS**

---

### テスト方針

- [ ] テストを書くか・書く場合どのレイヤーまでか（Unit / Integration / E2E）
- [ ] テストフレームワークを選定する（例: JUnit 5 / Vitest）

---

### 環境構築・開発スタート（次のアクション）

- [ ] ローカル開発環境を構築する（Docker使用有無を先に決める）
- [ ] GitリポジトリのブランチやCI/CD方針を決める
- [ ] デプロイパイプラインを設定する（Vercel / Railway）

---

## Phase 2（献立自動作成・家族グループ）

- [ ] 献立のER設計（`menus` / `menu_items` テーブル等）
- [ ] ポイント合計1.0の組み合わせアルゴリズムの詳細仕様を定義する
  - 完全に1.0にならない場合の許容範囲はあるか（例: 0.9〜1.1 を許容など）
  - レシピ数が不足している場合の具体的なエラー条件（何品以上必要か）
- [ ] 1品差し替え時のAPIおよびUX仕様
- [ ] 生成した献立の保存・閲覧のデータ設計
- [ ] 家族グループのER設計（`groups` / `group_members` テーブル等）
- [ ] グループ招待フローの詳細（招待リンク方式 or メール方式）
- [ ] レシピ画像登録の要否を改めて検討する

---

## Phase 3（食材・冷蔵庫機能）

- [ ] 食材・在庫のER設計（`ingredients` / `inventory` テーブル等）
- [ ] 「作った」マーク時の在庫減算ロジック仕様

---

## 確定済み事項

| 項目 | 決定内容 |
|---|---|
| フロントエンド | Vue 3 + Vite（Vue Router / Pinia） |
| バックエンド | Java / Spring Boot（Spring Security） |
| データベース | PostgreSQL |
| フロントホスティング | Vercel（無料枠） |
| バックエンド+DBホスティング | Railway（月$5〜） |
| 認証方式 | メールアドレス＋パスワード（JWT / Spring Security） |
| パスワードリセット方式 | DBに保存するランダムトークン（使い捨て） |
| カテゴリ管理 | `categories` テーブルで管理（将来の追加に対応） |
| レシピ削除 | 論理削除（`deleted_at`） |
| 画像登録 | Phase 1 はなし |
| ソーシャル認証 | スコープ外 |
| カロリー・栄養管理 | スコープ外 |
